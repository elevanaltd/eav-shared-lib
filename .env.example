# =============================================================================
# EAV SHARED LIB - TEST CREDENTIALS
# =============================================================================
#
# This file contains template environment variables for running tests locally.
# DO NOT commit actual credentials to git - copy this to .env and update values.
#
# Usage:
#   1. Copy this file: cp .env.example .env
#   2. Update .env with actual test credentials from Supabase
#   3. Run tests: npm test
#
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE TEST CREDENTIALS
# -----------------------------------------------------------------------------
# These credentials are used for RLS policy testing and authentication tests.
# They MUST match existing users in the Supabase auth.users table.
#
# Available test users (from Supabase query 2025-10-18):
#   - test-admin@elevana.com (admin role)
#   - test-client@external.com (client role, assigned to test-client-company)
#   - test-unauthorized@external.com (no client assignments)
#   - testclient@gmail.com (client role, assigned to Berkeley Group)
# -----------------------------------------------------------------------------

# Admin test user (has admin role in system)
TEST_ADMIN_EMAIL=test-admin@elevana.com
TEST_ADMIN_PASSWORD=your-admin-password-here

# Client test user (has client role with restricted access)
TEST_CLIENT_EMAIL=test-client@external.com
TEST_CLIENT_PASSWORD=your-client-password-here

# Unauthorized test user (exists but has no client assignments)
TEST_UNAUTHORIZED_EMAIL=test-unauthorized@external.com
TEST_UNAUTHORIZED_PASSWORD=your-unauthorized-password-here

# -----------------------------------------------------------------------------
# NOTES FOR CI/CD
# -----------------------------------------------------------------------------
# For GitHub Actions, set these as repository secrets:
#   Settings -> Secrets and variables -> Actions -> New repository secret
#
# The test suite will fail if these environment variables are not set.
# -----------------------------------------------------------------------------
