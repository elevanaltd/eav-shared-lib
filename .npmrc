# GitHub Packages registry configuration for @elevanaltd scope
# Constitutional mandate: CI_QUALITY_GATES (line 178) - must work in CI environment
@elevanaltd:registry=https://npm.pkg.github.com/
# Authentication token injected via environment variable (CI: GITHUB_TOKEN, Local: NPM_TOKEN)
# CI: GitHub Actions provides GITHUB_TOKEN automatically
# Local: Create Personal Access Token with read:packages scope
# Usage: echo "//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}" >> ~/.npmrc
